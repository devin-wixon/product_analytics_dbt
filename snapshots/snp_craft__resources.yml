version: 2

snapshots:
  - name: snp_craft__resources
    relation: source('craft', 'raw_craft__resources')
    config:
      unique_key: id
      strategy: check
      check_cols: "all"
      # strategy: timestamp
      # updated_at: updated_at::timestamp_ntz
      hard_deletes: "new_record"
      # dbt_valid_to_current:  "{{ var('dbt_valid_to_current') }}"
