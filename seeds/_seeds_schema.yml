version: 2

seeds:
  - name: seed_event_log_metadata
    description: Event name and BI metadata
    columns:
      - name: event_name
        data_tests:
          - unique
          - not_null
      - name: event_category
        data_tests:
          - not_null
      - name: event_capture_start_date
        data_tests:
          - not_null
      - name: event_capture_end_date
        data_tests:
          - not_null
