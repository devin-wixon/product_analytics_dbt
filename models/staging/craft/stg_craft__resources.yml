version: 2

models:
  - name: stg_craft__resources
    description: |
      Purpose: Resource data from the craft database with column typing and renaming.  
      Granularity: One row per resource, defined by `resource_id`.  
    columns:
      # source tests uniqueness and not null for resource_id; this will have dups due to snapshots
      - name: resource_id
