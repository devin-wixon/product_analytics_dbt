version: 2

models:
  - name: stg_taco__district_applications
    description: |
      Purpose: Raw district application data from the taco database with column typing and renaming.  
      Granularity: One row per district-application pair, defined by `district_application_id`.  
      Filters: None
    columns:
      # source tests uniqueness of district_application_id
      - name: district_application_id
      # source tests for non-null district_id, application_id
      # source tests for unique district_id x application_id combination
      - name: district_id
      - name: application_id
