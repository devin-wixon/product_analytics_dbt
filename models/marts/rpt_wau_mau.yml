version: 2

models:
  - name: rpt_wau_mau
    description: |
      Purpose: 
      Granularity: 
      Filters: Inherits student exclusion from upstream `rpt_obt_events_dims_daily`.
      Notes: 
    columns:
      # Date spine columns
      - name: date_day
        description: "{{ doc('date_day') }}"
        data_tests:
          - not_null
     