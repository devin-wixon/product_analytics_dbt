version: 2

models:
  - name: _rpt_user_events_date
    description: |
      Purpose: Daily summary of user activity, showing whether users engaged with specific features each day.
      Granularity: One row per user per calendar day.
      Filters: Inherited filter that user_role is not `student`
      Notes:
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ['user_id', 'event_date']
    columns:
      - name: user_id
        description: "{{ doc('user_id')}}"
 
    