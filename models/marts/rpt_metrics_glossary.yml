version: 2

models:
  - name: rpt_metrics_glossary
    description: |
      Purpose: Glossary of all dbt metrics defined in the project for reference and documentation.
      Granularity: One row per metric defined in the dbt project.
      Notes: Auto-generated from the dbt graph.metrics object during model compilation.
    columns:
      - name: metric_name
        data_tests:
          - unique
          - not_null
